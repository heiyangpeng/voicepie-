import type { App } from 'vue';
import type { Router } from './router';
import type { VitePressData } from './data';
export declare const setupDevtools: (app: App, router: Router, data: VitePressData) => void;
